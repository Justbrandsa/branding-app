import crypto from "crypto"; export const zToJSON=<T>(x:T)=>JSON.parse(JSON.stringify(x)); export function groupKey({productGid,method,positionsHash,artworkHash}:{productGid:string;method:string;positionsHash:string;artworkHash:string;}){return `jb:${productGid}:${method}:${positionsHash}:${artworkHash}`;} export function hashPositions(p:string[]){return crypto.createHash("sha1").update(p.sort().join(",")).digest("hex");}
