import { NextRequest, NextResponse } from "next/server"; export async function POST(req:NextRequest){ try{ const { variantId, qty, priceQuote, properties } = await req.json(); if(!variantId||!qty||!priceQuote) return NextResponse.json({error:"Invalid input"},{status:400}); const items=[ {id:priceQuote.setupFeeVariantGid,quantity:1,properties:{ jb_group_key: properties.jb_group_key }}, {id:priceQuote.perUnitFeeVariantGid,quantity:qty,properties:{ jb_group_key: properties.jb_group_key }}, {id:variantId,quantity:qty,properties} ]; return NextResponse.json({ items }); }catch(e:any){ return NextResponse.json({error:e.message},{status:500}); } }